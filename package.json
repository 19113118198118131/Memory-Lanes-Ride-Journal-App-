{
  "name": "memory-lanes",
  "version": "0.1.0",
  "private": true,
  "description": "Memory Lanes ride journal packaged with Capacitor for iOS.",
  "type": "module",
  "scripts": {
    "build:web": "node scripts/prepare-capacitor-web.mjs",
    "cap:add:ios": "pnpm run build:web && pnpm exec cap add ios",
    "cap:sync": "pnpm run build:web && pnpm exec cap sync ios",
    "ios:open": "pnpm exec cap open ios",
    "ios:run": "pnpm run build:web && pnpm exec cap run ios"
  },
  "dependencies": {
    "@capacitor/background-runner": "^3.0.0",
    "@capacitor/core": "^8.4.1",
    "@capacitor/geolocation": "^8.0.1",
    "@capacitor/ios": "^8.4.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.1",
    "typescript": "^5.9.3"
  }
}
