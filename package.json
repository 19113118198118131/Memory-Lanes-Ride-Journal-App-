{
  "name": "memory-lanes",
  "version": "0.1.0",
  "private": true,
  "description": "Memory Lanes ride journal packaged with Capacitor for iOS.",
  "type": "module",
  "scripts": {
    "build:web": "node scripts/prepare-capacitor-web.mjs",
    "cap:add:ios": "npm run build:web && npx cap add ios",
    "cap:sync": "npm run build:web && npx cap sync ios",
    "ios:open": "npx cap open ios",
    "ios:run": "npm run build:web && npx cap run ios"
  },
  "dependencies": {
    "@capacitor/background-runner": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/geolocation": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
